# VLLM – Local Avatar Talker

Minimal local app: upload a face image + text → TTS (Piper) → talking-head video (Wav2Lip).  
Modular providers so you can swap TTS/Video backends.

## Quick start
```bash
docker compose up --build
